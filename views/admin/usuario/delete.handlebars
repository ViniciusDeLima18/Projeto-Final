<h2>{{response.titulo}}</h2>

<form method="post">
    <hr>
    <p>Deseja realmente deletar o usuário <strong>{{response.data.nome}}</strong>
        <hr>
        <button type="subtmit" class="btn btn-success">Sim</button>
        <a href="/usuario" class="btn btn-danger">Não</a>
    </p>
</form>